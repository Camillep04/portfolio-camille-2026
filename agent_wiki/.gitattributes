# This wiki is written and regenerated by tooling that emits LF, and viz.html
# is a ~450 KB generated file. Pinning LF keeps a regeneration a no-op diff on
# every platform, and keeps the bundle byte-identical when it is copied into
# another project.
* text=auto eol=lf
